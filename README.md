# 🎓 Kursarium - Агрегатор онлайн-курсов

Статический сайт-агрегатор онлайн-курсов с автоматическим обновлением партнерских фидов каждые 6 часов.

## 🚀 Возможности

- ✅ **Автообновление фидов** - каждые 6 часов через GitHub Actions
- ✅ **Поддержка форматов** - XML, CSV, JSON
- ✅ **Масштабируемость** - до 5000+ курсов с пагинацией
- ✅ **Партнерские ссылки** - автоматическое добавление реф-параметров
- ✅ **Оптимизация логотипов** - автоматическое сжатие изображений
- ✅ **Статический сайт** - работает на GitHub Pages бесплатно

## 📁 Структура проекта

```
project/
├── index.html              # Главная страница
├── admin.html              # Админ-панель
├── package.json            # Node.js зависимости
│
├── css/                    # Стили
│   ├── style.css
│   └── admin.css
│
├── js/                     # JavaScript
│   ├── main.js
│   └── admin.js
│
├── data/                   # Данные курсов
│   ├── courses/
│   │   ├── page-1.json    # По 50 курсов на файл
│   │   ├── page-2.json
│   │   └── ...
│   ├── feeds-config.json  # Конфигурация фидов
│   └── meta.json          # Метаданные
│
├── images/                 # Изображения
│   └── schools/           # Логотипы школ
│
├── scripts/                # Node.js скрипты
│   └── import-feeds.js    # Импорт партнерских фидов
│
└── .github/
    └── workflows/
        └── update-feeds.yml  # GitHub Actions для автообновления
```

## ⚙️ Настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка фидов

Отредактируйте `data/feeds-config.json`:

```json
{
  "feeds": [
    {
      "id": "skillbox",
      "name": "Skillbox",
      "logo": "images/schools/skillbox.png",
      "logoUrl": "https://skillbox.ru/logo.png",
      "feedUrl": "https://skillbox.ru/feed.xml",
      "feedFormat": "xml",
      "affiliateParam": "?ref=your-id",
      "enabled": true
    }
  ],
  "settings": {
    "updateInterval": "0 */6 * * *",
    "maxCoursesPerPage": 50
  }
}
```

### 3. Тестовый импорт

Запустите импорт локально для проверки:

```bash
npm run import-feeds
```

### 4. Деплой на GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-юзернейм/kursarium.git
git push -u origin main
```

3. Включите GitHub Pages:
   - Settings → Pages
   - Source: Deploy from branch `main`
   - Folder: `/` (root)

4. GitHub Actions автоматически запустится каждые 6 часов

## 🔄 Как работает автообновление

1. **GitHub Actions** запускается по расписанию (каждые 6 часов)
2. **Node.js скрипт** (`scripts/import-feeds.js`):
   - Скачивает фиды по URL из конфига
   - Парсит XML/CSV/JSON
   - Скачивает и оптимизирует логотипы школ
   - Добавляет партнерские параметры к ссылкам
   - Разбивает курсы по файлам (50 на страницу)
   - Сохраняет в `data/courses/page-*.json`
3. **Коммит изменений** - автоматически пушит обновления
4. **GitHub Pages** - автоматически публикует новую версию

## 📝 Формат фидов

### XML (RSS)
```xml
<?xml version="1.0"?>
<rss version="2.0">
  <channel>
    <item>
      <title>Python-разработчик</title>
      <link>https://school.com/course</link>
      <price>89900</price>
      <oldPrice>150000</oldPrice>
      <discount>40</discount>
      <category>Программирование</category>
    </item>
  </channel>
</rss>
```

### JSON
```json
{
  "courses": [
    {
      "id": "123",
      "name": "Python-разработчик",
      "url": "https://school.com/course",
      "price": "89900",
      "oldPrice": "150000",
      "category": "Программирование"
    }
  ]
}
```

### CSV
```csv
id,name,url,price,oldPrice,category
123,Python-разработчик,https://school.com/course,89900,150000,Программирование
```

## 🛠️ Локальная разработка

1. Откройте `index.html` в браузере
2. Для работы с динамическими данными запустите локальный сервер:
```bash
npx http-server
```

## 📊 Админ-панель

Откройте `admin.html` для:
- Просмотра статистики
- Ручного импорта фидов
- Управления курсами

## 🔧 Команды npm

```bash
npm run import-feeds  # Запустить импорт фидов
npm run test-import   # Тестовый импорт (без сохранения)
```

## 📅 Расписание обновлений

По умолчанию: **каждые 6 часов** (00:00, 06:00, 12:00, 18:00 UTC)

Изменить можно в `.github/workflows/update-feeds.yml`:
```yaml
schedule:
  - cron: '0 */6 * * *'  # Формат: минута час день месяц день_недели
```

Примеры:
- `0 */3 * * *` - каждые 3 часа
- `0 0 * * *` - раз в день в полночь
- `0 */12 * * *` - каждые 12 часов

## 💡 Советы

1. **Тестируйте локально** перед деплоем
2. **Проверяйте логи** в GitHub Actions
3. **Оптимизируйте логотипы** - не более 200x200px
4. **Кешируйте фиды** - не загружайте слишком часто

## 📄 Лицензия

MIT License - используйте свободно для коммерческих проектов

## 🤝 Поддержка

Если возникли вопросы - создайте Issue в репозитории.
