# 🚀 Инструкция по загрузке на GitHub

## Шаг 1: Создай репозиторий на GitHub

1. Открой: https://github.com/new
2. Заполни:
   - **Repository name:** `kursarium` (или любое название)
   - **Description:** `Агрегатор онлайн-курсов`
   - **Public** ✓
   - **НЕ добавляй** README, .gitignore, license
3. Нажми **Create repository**

---

## Шаг 2: Скопируй команды для загрузки

После создания репозитория GitHub покажет команды. Выполни их в PowerShell:

### Вариант 1: Если Git уже инициализирован (как у нас)

```bash
git add .
git commit -m "🚀 Initial commit - Kursarium aggregator"
git branch -M main
git remote add origin https://github.com/ВАШ-USERNAME/kursarium.git
git push -u origin main
```

**⚠️ ВАЖНО:** Замени `ВАШ-USERNAME` на свой GitHub username!

---

### Вариант 2: Полная последовательность команд

Скопируй и выполни по очереди в PowerShell (в папке проекта):

```powershell
# 1. Инициализация Git (если еще не сделано)
git init

# 2. Добавление всех файлов
git add .

# 3. Создание коммита
git commit -m "🚀 Initial commit - Kursarium with 26 Contented courses"

# 4. Переименование ветки в main
git branch -M main

# 5. Подключение удаленного репозитория
# ⚠️ ЗАМЕНИ URL НА СВОЙ!
git remote add origin https://github.com/ВАШ-USERNAME/kursarium.git

# 6. Загрузка на GitHub
git push -u origin main
```

---

## Шаг 3: Включи GitHub Pages

1. Открой свой репозиторий на GitHub
2. Перейди в **Settings** (⚙️)
3. В левом меню найди **Pages**
4. В разделе **Source:**
   - **Branch:** выбери `main`
   - **Folder:** выбери `/ (root)`
5. Нажми **Save**

**Готово!** Через 1-2 минуты сайт будет доступен:
```
https://ВАШ-USERNAME.github.io/kursarium/
```

---

## Шаг 4: Проверь автообновление

1. В репозитории открой вкладку **Actions**
2. Найди workflow **"Auto Update Course Feeds"**
3. Нажми **Run workflow** → **Run workflow** для тестового запуска
4. Дождись завершения (1-2 минуты)
5. Проверь что фиды обновились

**Теперь каждые 6 часов курсы будут обновляться автоматически!** ⏰

---

## Проблемы?

### Ошибка: "remote origin already exists"

```bash
git remote remove origin
git remote add origin https://github.com/ВАШ-USERNAME/kursarium.git
git push -u origin main
```

### Ошибка: "authentication failed"

Нужно настроить Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Выбери права: `repo`
4. Используй токен вместо пароля при git push

### Ошибка: "src refspec main does not match any"

```bash
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
```

---

## 🎉 Готово!

После успешной загрузки:
- ✅ Код на GitHub
- ✅ Сайт работает на GitHub Pages
- ✅ Автообновление каждые 6 часов
- ✅ 26 курсов от Contented

**Начни зарабатывать на партнерских программах! 💰**
